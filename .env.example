PORT=3000

# Number of reverse proxy hops in front of this server (e.g. nginx).
# Needed so express-rate-limit can trust X-Forwarded-For for the real client IP.
TRUST_PROXY_HOPS=1

# Rate limiting for /lorem/* and /nonsense/* (per IP)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MINUTES=15
